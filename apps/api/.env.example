# Copy from repo root or set here when running from apps/api only.
# DATABASE_URL is read from root .env when running via npm run dev:api from monorepo root.
# For local override (optional):
# DATABASE_URL="postgresql://user:password@localhost:5432/jhingula"
# PORT=3001
